# Packer secrets — copy to secrets.auto.pkrvars.hcl and fill in values.
# Do NOT commit secrets.auto.pkrvars.hcl — it is gitignored.

vcenter_password = "your-vcenter-password"

# Runner VM passwords (must match cloud-init user-data / autounattend.xml)
ssh_password   = "packer"
winrm_password = "Packer@2024!"
