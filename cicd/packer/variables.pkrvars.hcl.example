# vSphere connection settings
# Copy this file to variables.pkrvars.hcl and fill in your values.
# Do NOT commit variables.pkrvars.hcl — it is gitignored.

vcenter_server     = "vcenter.example.com"
vcenter_username   = "administrator@vsphere.local"
vcenter_insecure   = true
vcenter_datacenter = "Datacenter"
vcenter_cluster    = "Cluster"
vcenter_datastore  = "datastore1"
vcenter_network    = "VM Network"
vcenter_folder     = "Templates"

# ISO sources are NOT set here — each template defaults its own iso_url so this
# shared var file can't accidentally feed one OS's ISO to the other build:
#   - Linux:   repack-iso.sh downloads + repacks the Ubuntu source on demand.
#   - Windows: Packer downloads the eval ISO straight from the default iso_url.
# Override per build with `-var iso_url=...` only if you need a different image.

# Runner agent version
github_runner_version = "2.321.0"
