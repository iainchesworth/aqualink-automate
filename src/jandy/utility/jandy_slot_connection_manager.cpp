#include "utility/jandy_slot_connection_manager.h"

namespace AqualinkAutomate::Utility
{

	// JandySlotConnectionManager is header-only (it only adds an inline,
	// templated RegisterSlot_FilterByDeviceId on top of the shared
	// SlotConnectionManager base, whose ctor/dtor live in the core library).

}
// namespace AqualinkAutomate::Utility
